# Server port
PORT=3000

# Security: Generate a secure random string for your API key
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('base64').replace(/[+/=]/g, ''))"
API_KEY=your_secure_api_key_here

# Frontend URL for CORS
CORS_ORIGIN=http://localhost:5173 